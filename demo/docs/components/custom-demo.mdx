---
title: Custom Component Demo
description: Interactive demo of custom React components
---

# Custom Component Demo

This page demonstrates a custom React component with performance optimizations.

<CustomDemo />

## Implementation Details

The custom component uses:

- `useMemo` for expensive computations
- `useCallback` for event handlers
- Modular structure to prevent re-renders

